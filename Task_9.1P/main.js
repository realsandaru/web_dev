import { createApp } from 'vue'; 
import App from './App.vue'; 
// Create a new Vue application instance
const app = createApp(App);
// Tell Vue where to show the app on your webpage
app.mount('#app');
